export default function Footer(){
    return(
        <footer style={styles.container}>
            <h1>logo</h1>
            <h1> esse é o footer</h1>
        </footer>
    )
}

const styles = {
    container:{
        backgroundColor: "#fff",
        color: "#000"
    }
}